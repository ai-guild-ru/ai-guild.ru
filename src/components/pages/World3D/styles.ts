import { LayoutInnerContainer } from 'src/components/Layout/styles'
import { createGlobalStyle } from 'styled-components'

export const World3DScenePageGlobalStyles = createGlobalStyle`
  ${LayoutInnerContainer} {
    max-width: none;
    padding: 0;
  }
`
